name: test

on:
  push:
    branches:
    - develop
  pull_request:

jobs:

  test-client:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: cd client && npm install
    - run: cd client && npm test:unit
